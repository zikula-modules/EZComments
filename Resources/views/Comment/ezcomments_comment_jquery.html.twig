{{ pageAddAsset('javascript', zasset('@ZikulaEZCommentsModule:js/Zikula.EZComments.GetComments.js')) }}
{{ pageAddAsset('stylesheet', zasset('@ZikulaEZCommentsModule:css/style.css')) }}
<div class="container-fluid">
    <div class="row">
        <h1>A test of Ajax functions</h1>
        <div id="content">

            <p>The <code>$.ajax()</code> function is used to perform basic AJAX requests. It can be customized using a variety of parameters.</p>
            <p>If successful, the returned data will appear here:</p>
            <button id="twiddle">Press me</button>
            <p id="ajaxContent"></p>
        </div>
    </div><!-- row -->
</div>